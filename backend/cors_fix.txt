from fastapi.middleware.cors import CORSMiddleware

# Fix CORS - tidak boleh pakai "*" dengan credentials=True
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],  # Spesifik origin
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
